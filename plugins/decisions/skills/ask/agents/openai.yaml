interface:
  display_name: "Decisions: ask"
  short_description: "Screen many files with a closed question via the decide CLI"
  default_prompt: "Use $ask to run a decisions spec over this repo and report what it kept."
policy:
  allow_implicit_invocation: true
