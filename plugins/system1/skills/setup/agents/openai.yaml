interface:
  display_name: "Decisions: setup"
  short_description: "Set up or troubleshoot the decide CLI, key, consent and network"
  default_prompt: "Use $setup to check the decisions setup and fix what is missing."
policy:
  allow_implicit_invocation: false
