{
  "name": "@garygentry/decisions",
  "version": "0.0.0",
  "description": "The `decide` CLI: typed, calibrated decisions for coding agents, hooks and CI.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/garygentry/decisions",
    "directory": "packages/cli"
  },
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "decide": "./dist/bundle/decide.mjs"
  },
  "files": [
    "dist/bundle"
  ],
  "scripts": {
    "build": "tsc -b && node bundle.mjs"
  },
  "devDependencies": {
    "@garygentry/decisions-core": "workspace:*",
    "esbuild": "^0.28.2"
  }
}
